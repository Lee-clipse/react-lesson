import React, { useState, useEffect } from "react";

function Index1001() {
  return (
    <div>
      <div id="lesson-title">인자</div>
      <div id="lesson-content">
        <div className="title">목차</div>
        <div>1. 이미 분리된 함수에 대해 인자를 사용할 수 있다.</div>
        <div>2. 함수를 직접 분리하여 인자를 사용할 수 있다.</div>
        <div>3. 컴포넌트를 직접 분리하여 props를 사용할 수 있다.</div>
      </div>
    </div>
  );
}

export default Index1001;
